# Floors are chosen so the whole scientific stack is numpy-2.x-compatible.
# (Do NOT pin numpy<2 — newer Streamlit Cloud images run Python 3.13, which has
# no numpy 1.x wheels, so a <2 pin makes the build fail to install.)
# Raising pandas/scipy/sklearn floors to their first numpy-2-aware releases
# avoids the ABI mismatch (numpy 2.x runtime + a package built against 1.x)
# that crashed the pandas import.
streamlit>=1.43,<1.58  # 1.57.x line the layout/nav CSS was verified against
yfinance>=0.2.40
curl_cffi>=0.7  # browser impersonation for yfinance so Yahoo doesn't rate-limit cloud IPs
anthropic>=0.40  # powers the in-app Jarvis assistant (needs ANTHROPIC_API_KEY secret)
pillow>=10  # downscales the password-screen background image
numpy>=1.26
pandas>=2.2.2  # first pandas with numpy-2 ABI support
plotly>=5.18
scipy>=1.13  # numpy-2 compatible
statsmodels>=0.14.2
scikit-learn>=1.4
